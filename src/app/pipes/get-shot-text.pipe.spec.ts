import { GetShotTextPipe } from './get-shot-text.pipe';

describe('GetShotTextPipe', () => {
  it('create an instance', () => {
    const pipe = new GetShotTextPipe();
    expect(pipe).toBeTruthy();
  });
});
